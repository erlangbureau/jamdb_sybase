{lib_dirs, ["deps"]}.

{erl_opts, [
]}.

{deps, [ 
]}.
  
{xref_checks, [undefined_function_calls]}.

{cover_enabled, true}.

{eunit_opts, [
    verbose,
    {report, {eunit_surefire, [{dir, "."}]}}
]}.

{clean_files, ["deps", "ebin"]}.
